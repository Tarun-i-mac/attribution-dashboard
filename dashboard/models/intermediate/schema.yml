version: 1

models:
  - name: int_customer_touchpoints
    columns:
      - name: user_pseudo_id
        tests: [not_null]
      - name: event_ts
        tests: [not_null]

  - name: int_completed_purchases
    columns:
      - name: user_pseudo_id
        tests: [not_null]
      - name: event_ts
        tests: [not_null]